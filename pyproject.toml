[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "divisor"
version = "0.1.0"
description = "fragment / denoise / process"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "einops>=0.8.1",
    "fire>=0.7.1",
    "opencv-python>=4.12.0.88",
    "pillow>=12.0.0",
    "sentencepiece>=0.2.1",
    "nnll",
]
[project.scripts]
dvzr = "divisor.flux_modules.prompt:main"


[dependency-groups]
dev = [
    "pyright>=1.1.407",
    "ruff>=0.14.5",
]
[tool.pyright]
include = ["divisor"]
exclude = [
  "**/__pycache__", # cache directories
  "./typings",      # generated type stubs
]
[tool.setuptools]
packages = ["divisor"]

[tool.setuptools.package-data]
"*" = ["*.txt", "*.md"]

[tool.uv]
package = true
preview = true

[tool.uv.sources]
nnll = { git = "https://github.com/darkshapes/nnll" }
